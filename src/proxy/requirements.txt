# ===== Core Networking =====
requests==2.31.0              # Gửi HTTP/HTTPS request, hỗ trợ TLS/SSL
urllib3==2.1.0                # Dependency chính của requests
certifi==2024.7.4             # Bộ CA gốc để verify HTTPS
idna==3.7                     # Domain name encoding
charset-normalizer==3.3.2     # Đọc mã hóa text

# ===== Dev Tools & Logging =====
python-dotenv==1.0.1          # Đọc cấu hình từ file .env (ví dụ PORT, LOG_LEVEL)
colorama==0.4.6               # In màu terminal dễ đọc (log/debug)
loguru==0.7.2                 # Logging đẹp, rõ ràng

# ===== Optional / Debug =====
rich==13.9.2                  # Giao diện log màu, progress bar, debug table

