========================================
FIX CHO STDAFX.H - VC++ 6.0 COMPATIBILITY
========================================

Tìm dòng này trong stdafx.h (khoảng dòng 207-214):

//--- Debug Logging ---
#ifdef _DEBUG
    #define DEBUG_LOG(msg)      std::cout << "[DEBUG] " << msg << std::endl
    #define DEBUG_LOGF(fmt,...) printf("[DEBUG] " fmt "\n", __VA_ARGS__)
#else
    #define DEBUG_LOG(msg)      ((void)0)
    #define DEBUG_LOGF(fmt,...) ((void)0)
#endif

THAY BẰNG:

//--- Debug Logging ---
#ifdef _DEBUG
    #define DEBUG_LOG(msg)      std::cout << "[DEBUG] " << msg << std::endl
    // VC++ 6.0 doesn't support variadic macros well, use inline function instead
    inline void DEBUG_LOGF(const char* fmt, ...) {
        printf("[DEBUG] ");
        va_list args;
        va_start(args, fmt);
        vprintf(fmt, args);
        va_end(args);
        printf("\n");
    }
#else
    #define DEBUG_LOG(msg)      ((void)0)
    inline void DEBUG_LOGF(const char*, ...) {}
#endif

========================================
SAU KHI SỬA XONG, CHẠY LẠI b.bat
========================================
